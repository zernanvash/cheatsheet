A Windows CTF for anyone interested in dealing with a binary that requires a mixed approach and circumventing various anti-tamper mechanisms. Should be pretty straightforward to do once you do some initial RE.

The program can be run, just remove the .bin extension; though please do so in a controlled/isolated environment.

